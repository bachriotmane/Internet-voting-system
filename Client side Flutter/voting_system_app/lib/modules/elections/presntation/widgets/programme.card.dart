import 'package:flutter/material.dart';

class ProgrammeCard extends StatelessWidget {
  const ProgrammeCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Programme Card"),
    );
  }
}
